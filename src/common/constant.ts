export enum SyntaxKind {
    Text = 'text',
    Tag = 'tag',
}

export enum SymbolTag {
    OFor = 'o-for',
    OIf = 'o-if',
    OElseIf = 'o-else-if',
    OElse = 'o-else',
    OInclude = 'o-include',
    OText = 'o-text',
}
